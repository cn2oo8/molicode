import actions from './actions';
import rules from './rules';
import urls from './urls';
import dicts from './dicts';
import bizKeys from './bizKeys';
import * as types from './types';

export default {
    actions,
    rules,
    urls,
    dicts,
    types,
    bizKeys
}
